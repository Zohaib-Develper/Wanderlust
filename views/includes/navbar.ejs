<nav
  class="navbar navbar-expand-lg bg-white sticky-top container-fluid p-3 border-bottom"
>
  <div class="container-fluid">
    <i class="fa-2x fa-brands fa-airbnb text-danger fw-bolder"></i>
    <a class="navbar-brand text-danger ms-1" href="/">airbnb</a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link fw-bold" href="/listings">Explore</a>
      </div>
      <div class="ms-auto">
        <form method="GET" action="/listings" class="d-flex" role="search">
          <input
            class="form-control me-2"
            type="text"
            placeholder="Search destinations"
            aria-label="Search"
            name="search"
          />
          <button class="btn btn-outline-danger">Search</button>
        </form>
      </div>
      <div class="navbar-nav ms-auto">
        <a class="nav-item nav-link fw-bold text-danger" href="/listings/new"
          >Airbnb your home</a
        >
        <%if(!curUser){%>
        <a class="nav-item nav-link fw-bold" href="/login">Login </a>
        <a class="nav-item nav-link fw-bold" href="/signup">SignUp</a>
        <%} else{%>
        <a class="nav-item nav-link fw-bold" href="/logout">Logout</a>
        <%}%>
      </div>
    </div>
  </div>
</nav>
